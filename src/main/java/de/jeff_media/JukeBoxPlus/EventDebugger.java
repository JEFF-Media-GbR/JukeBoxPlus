package de.jeff_media.JukeBoxPlus;

import org.bukkit.event.inventory.InventoryClickEvent;

public class EventDebugger {



    static void debug(InventoryClickEvent e, Main m) {

       /* m.debug("===== InventoryClickEvent =====");
        m.debug("Action: "+e.getAction().name());
        m.debug("Click: "+e.getClick().name());
        m.debug("Inventory: "+e.getInventory());
        m.debug("Clicked Inv: "+e.getClickedInventory());
        m.debug("Current Item: "+e.getCurrentItem());
        m.debug("Cursor: "+e.getCursor());
        m.debug("Hotbar Button: "+e.getHotbarButton());
        m.debug("Slot: "+e.getSlot());
        m.debug("Raw slot: "+e.getRawSlot());
        m.debug("Slottype: "+e.getSlotType().name());
        m.debug("Leftclick: "+e.isLeftClick());
        m.debug("Rightclick: "+e.isRightClick());
        m.debug("Shiftclick: "+e.isShiftClick());*/
    }

}
